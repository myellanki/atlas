build a project management software that integrates the dashboard for project planning and the cards/task layout used in microsoft teams. All existing functionalities should remain, With that, there should be an integration to create visually digestible gantt charts that will automatically update as the associated projects in the dashboard have updated dates. This will be used to manage a group of data scientists split into various teams where each team will have their own page. Within each page, each analyst on that team should have their own column that can have a card for each of their current projects. Just like in teams, there should be the ability to designate the project priority, status, due date, assignment, notes (unlimited) that can be shown on the card even when it is not clicked on, a checklist, comment section, ability to link anything external or other cards/pages within the dashboard, and the ability to use labels to label the cards between projects and pages (unlimited would be helpful). The software should also be able to look at the most recent note put into the card and for each column, be able to generate a simple table with the card/project title, the column it is under, and a brief 5-10 word summary of the most recent update and upcoming deadlines. Backend: use local browser storage an company github/servers
Auth & access: Admin + Member roles
AI summaries: AI-generated on demand
Visual style: Other: mix between microsoft teams-inspired and clean light & minimal
Notes: Dashboard — KPI tiles, mini Gantt timeline, per-team summary tables, recent activity feed
Team pages (/teams/...) — one column per analyst, Teams-style cards with priority dot, status/priority chips, label pills, latest-note preview shown right on the card, due date, checklist count, comments, and links — all visible without opening
Card detail — status, priority, assignee, start/due dates, unlimited labels (toggleable), unlimited notes (latest highlighted), checklist, comments, and links that can point to external URLs or other cards in Atlas (click to jump)
Gantt (/gantt) — auto-updates from card start/due dates, today line, overdue outline, team filter
Per-column AI summary table — bottom of every team page and on dashboard; "Generate summaries" produces a 5-10 word digest of each card's most recent note plus the upcoming deadline
Labels page for unlimited cross-team tags, Settings for team/member management, role-gated editing throughout
make it so that the gantt and dashboard has a whole page per team, and then each column in that page will get its own gantt chart based on cards/projects in that column of that page of the dashboard
Each team now has its own Gantt page at /gantt/$teamId with one mini Gantt per analyst column, plus a team-wide timeline; the dashboard shows a per-team Gantt preview, and team boards have a "Team Gantt" button.
I would like to show the mini gantts per analyst mainly.
For the dashboard — the per-team section can show a single team-wide GanttPreview, but also needs to show a mini Gantt per analyst column inside each team section.
Make the app and workflow efficient and make sure the following error does not occur: Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.
Can consider the following solution: Both fixes applied: the store now memoizes per-hook-instance with shallow equality (no more infinite loop), and the root component defers rendering until mount (no more hydration mismatch).
n addition to these, create a dashboard/Board page that has the project cards/tasks similar to how they look on miscrosoft teams with all the same functionalities. Allow for each team (dsg1, dsg2, ml, and nlp) to have their own board with each team member having their own column in that board. The cards/their projects will line up under that column and should be moveable around the board. The gantt charts should also be able to displayed if clicked on in that board for a quick view and easy changes to any dates to update the gantt chart.